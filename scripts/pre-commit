#!/bin/sh
# Pre-commit hook to ensure the project builds before committing

echo "Running pre-commit build check..."

npm run build

if [ $? -ne 0 ]; then
  echo "Build failed. Commit aborted."
  exit 1
fi

echo "Build succeeded. Proceeding with commit."
exit 0
