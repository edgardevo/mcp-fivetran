FIVETRAN_API_KEY=your_api_key_here
FIVETRAN_API_SECRET=your_api_secret_here
CENSUS_API_KEY=your_census_api_key_here

# Opt in to write tools (create_connector, update_connector, sync_connector, resync_connector, run_connection_tests).
# Leave empty / unset for read-only mode. Truthy: true, 1, yes (case-insensitive).
FIVETRAN_ALLOW_WRITES=
